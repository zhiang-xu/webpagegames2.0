fix: add overflow-y:auto to body and fix align-items for mobile scroll on 5 games

- snake: body align-items center->flex-start, add overflow-y:auto (content was unscrollable on mobile)
- gomoku: add overflow-y:auto to body (700px breakpoint already switches to flex-start)
- xiangqi: add overflow-y:auto to body (700px breakpoint already switches to flex-start)
- junqi: add overflow-y:auto to body
- game2048: add overflow-y:auto to body

Root cause: body with min-height:100vh + align-items:center traps content
below the fold with no way to scroll on small viewports.
