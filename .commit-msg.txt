fix: correct game-ui.js default homeHref and remove duplicate back/sound buttons

- Fix js/game-ui.js default homeHref from '../index.html' to '../../index.html'
- Remove duplicate hard-coded back-btn and sound-toggle elements from
  gomoku, xiangqi, and doushouqi HTML (now handled by game-ui.js)
- Remove dead .back-btn, .sound-toggle, .icon-btn CSS from those 3 games
- Fix doushouqi topbar layout after removing icon buttons
